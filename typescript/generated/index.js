// @platoorg/ts-client — stub
// Run `npx plato-ts generate` to generate the typed client from your manifest.
export class PlatoClient {
  constructor() {
    throw new Error(
      '[plato] Client not generated.\n' +
      'Run `npx plato-ts generate` in your project root to generate the typed client.',
    );
  }
}
